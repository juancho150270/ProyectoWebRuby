<%= form_with(model: tracing_type, local: true) do |form| %>
  <% if tracing_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tracing_type.errors.count, "error") %> prohibited this tracing_type from being saved:</h2>

      <ul>
      <% tracing_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <div class="form-row">
      <div class="col-md-3">

        <label id="lblId">Id:</label>
        <input type="number" id="tracing_type_id" name="tracing_type[id]" 
        class="form-control" placeholder="id Automatico" readonly="readonly"
        value="<%= @tracing_type != nil ? @tracing_type.id : '' %>">
        
      </div>
      
      <div class="col-md-3">
        
        <label id="lbldescripcion">Descripción:</label>
        <input type="text" id="tracing_type_descripcion" name="tracing_type[descripcion]" 
        class="form-control" placeholder="Ingrese descripción" required="required"
        value="<%= @tracing_type != nil ? @tracing_type.descripcion : '' %>">

      </div>

      <div class="col-md-3">
        
        <label id="lblfecha_creacion">Fecha creación:</label>
        <input type="text" id="tracing_type_fecha_creacion" name="tracing_type[fecha_creacion]" 
        class="form-control" placeholder="Fecha Automatico" readonly="readonly"
        value="<%= @tracing_type != nil ? @tracing_type.fecha_creacion : '' %>">

      </div>

    </div>
  </div>

  <div class="form-group">
    <div class="form-row">
      <div class="col-md-12">
        
        <input type="submit" id="btnAceptar" value="Aceptar"
        class="btn btn-primary" name="btnAceptar"/>

      </div>
    </div>
  </div>

<% end %>  