<p id="notice"><%= notice %></p>

<div class="container">
  <div class="card mx-auto mt-5">
    <div class="card-header">Tipo de seguimiento</div>
    <div class="card-body">

 <div class="form-group">
    <div class="form-row">
      <div class="col-md-6">
        <%= link_to 'Nuevo', new_tracing_type_path,{ class: "btn btn-warning btn-sm" } %>
      </div>
    </div>
  </div>
  <div class="form-group">
    <div class="form-row">
      <div class="col-md-6">

        <table class="table table-bordered">
          <thead>
            <tr>
              <th>Id</th>
              <th>Descripcion</th>
              <th>Fecha creación</th>
              <th colspan="3"></th>
            </tr>
          </thead>
          <tbody>
            <% @tracing_types.each do |tracing_type| %>
              <tr>
                <td><%= tracing_type.id %></td>
                <td><%= tracing_type.descripcion %></td>
                <td><%= tracing_type.fecha_creacion %></td>
                <td><%= link_to 'Ver', tracing_type,{ class: "btn btn-info btn-sm" } %></td>
                <td><%= link_to 'Editar', edit_tracing_type_path(tracing_type),{ class: "btn btn-success btn-sm" } %></td>
                <td><%= link_to 'Eliminar', tracing_type,:class=>"btn btn-danger btn-sm", method: :delete, data: { confirm: '¿Realmente desea eliminar el registro?' } %></td>
              </tr>
            <% end %>
          </tbody>
        </table>

      </div>
    </div>
  </div>

    </div>
  </div>
</div>
