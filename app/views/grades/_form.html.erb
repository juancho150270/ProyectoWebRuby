<%= form_with(model: grade, local: true) do |form| %>
  <% if grade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(grade.errors.count, "error") %> prohibited this grade from being saved:</h2>

      <ul>
      <% grade.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion, id: :grade_descripcion %>
  </div>

  <div class="field">
    <%= form.label :fecha_creacion %>
    <%= form.datetime_select :fecha_creacion, id: :grade_fecha_creacion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
