<p id="notice"><%= notice %></p>

<div class="container">
  <div class="card mx-auto mt-5">
    <div class="card-header">Genero</div>
    <div class="card-body">

 <div class="form-group">
    <div class="form-row">
      <div class="col-md-6">
        <%= link_to 'Nuevo', new_gender_path,{ class: "btn btn-warning btn-sm" } %>
      </div>
    </div>
  </div>
  <div class="form-group">
    <div class="form-row">
      <div class="col-md-6">

        <table class="table table-bordered">
          <thead>
            <tr>
              <th>Id</th>
              <th>Descripcion</th>
              <th>Fecha creación</th>
              <th colspan="3"></th>
            </tr>
          </thead>
          <tbody>
            <% @genders.each do |gender| %>
              <tr>
                <td><%= gender.id %></td>
                <td><%= gender.descripcion %></td>
                <td><%= gender.fecha_creacion %></td>
                <td><%= link_to 'Ver', gender,{ class: "btn btn-info btn-sm" } %></td>
                <td><%= link_to 'Editar', edit_gender_path(gender),{ class: "btn btn-success btn-sm" } %></td>
                <td><%= link_to 'Eliminar', gender,:class=>"btn btn-danger btn-sm", method: :delete, data: { confirm: '¿Realmente desea eliminar el registro?' } %></td>
              </tr>
            <% end %>
          </tbody>
        </table>

      </div>
    </div>
  </div>

    </div>
  </div>
</div>
