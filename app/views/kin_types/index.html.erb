<p id="notice"><%= notice %></p>

<div class="container">
  <div class="card mx-auto mt-5">
    <div class="card-header">Tipo de parentesco</div>
    <div class="card-body">

 <div class="form-group">
    <div class="form-row">
      <div class="col-md-6">
        <%= link_to 'Nuevo', new_kin_type_path,{ class: "btn btn-warning btn-sm" } %>
      </div>
    </div>
  </div>
  <div class="form-group">
    <div class="form-row">
      <div class="col-md-6">

        <table class="table table-bordered">
          <thead>
            <tr>
              <th>Id</th>
              <th>Descripcion</th>
              <th>Fecha creación</th>
              <th colspan="3"></th>
            </tr>
          </thead>
          <tbody>
            <% @kin_types.each do |kin_type| %>
              <tr>
                <td><%= kin_type.id %></td>
                <td><%= kin_type.descripcion %></td>
                <td><%= kin_type.fecha_creacion %></td>
                <td><%= link_to 'Ver', kin_type,{ class: "btn btn-info btn-sm" } %></td>
                <td><%= link_to 'Editar', edit_kin_type_path(kin_type),{ class: "btn btn-success btn-sm" } %></td>
                <td><%= link_to 'Eliminar', kin_type,:class=>"btn btn-danger btn-sm", method: :delete, data: { confirm: '¿Realmente desea eliminar el registro?' } %></td>
              </tr>
            <% end %>
          </tbody>
        </table>

      </div>
    </div>
  </div>

    </div>
  </div>
</div>
